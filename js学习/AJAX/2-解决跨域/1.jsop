JSONP
    只支持get请求
    在网页有一些标签天生具有跨域能力，img script iframe link
    JSONP就是利用script标签的跨域能力来发送请求的
使用：
1.动态的创建一个script标签
    var script = document.createElement('script');
2.设置script的src，设置回调函数
    script.src = 'http://localhost:8002/testAJAX?callback=abc'